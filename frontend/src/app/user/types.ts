export interface User {
    user_id: number;
    user_name: string;
    user_picture: string;
    user_profile: string;
    // 他の必要なプロパティをここに追加
}

export interface Photo {
    photo_id: number;
    photo_data: string;
    // 他の必要なプロパティをここに追加
}
